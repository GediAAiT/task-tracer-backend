PORT=3000

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=changeme
DB_DATABASE=task_tracer_backend_db

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# Demo switch. When true, writes stop retiring cached list pages, so
# GET /tasks keeps serving its last snapshot until the TTL expires and a
# newly created task is missing from the list. Leave unset in production.
TASKS_CACHE_BREAK_INVALIDATION=false
